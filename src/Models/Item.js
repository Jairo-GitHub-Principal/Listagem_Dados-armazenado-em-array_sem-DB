/** abaixo temos uma classe que usaremos para armazenar os dados 
 * o qual usaremos pra fazer nossa listagem do tempos cronometrados
 */


export default class Item{
    constructor(nome,preco,quantidade,id){

        this.nome = nome,
        this.preco = preco,
        this.quantidade = quantidade,
        this.id = id
        
       
       

    }
}